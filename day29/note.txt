模板引擎(artTemplate)：
	a. 定义模板
		<script type="text/html" id="xx"></script>
	b. 模板语法：
		简洁 -- template.js
			使用 {{ }} 包含JS代码
		原生 -- template-native.js
			使用 <% %> 包含JS代码
	c. 渲染：
		var html = template(id, data)